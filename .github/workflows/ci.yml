name: CI

on:
  push:
    branches: [main, dev]
  pull_request:
    branches: [main, dev]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Build core modules
        run: ./gradlew :aow2-common:build :aow2-core:build :aow2-server:build :aow2-modding:build -x test

      - name: Run tests
        run: ./gradlew :aow2-common:test :aow2-core:test :aow2-server:test :aow2-modding:test
        env:
          JAVA_TOOL_OPTIONS: "-Djava.awt.headless=true"

      - name: Generate JaCoCo report
        run: ./gradlew :aow2-common:jacocoTestReport :aow2-core:jacocoTestReport :aow2-server:jacocoTestReport :aow2-modding:jacocoTestReport

      - name: Upload test results
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: test-results
          path: '**/build/reports/tests/'

      - name: Upload coverage report
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: coverage-report
          path: '**/build/reports/jacoco/'
